import { NavLink } from "react-router-dom"

export default  function Navbar () {
    return(
        <>
        <NavLink to = "/" > accueil </NavLink>
        <NavLink to = "/jadopte" > j'adopte </NavLink>
        </>
    )

}
